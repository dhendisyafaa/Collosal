---
import Navbar from "../components/Navbar.astro";
let show = false


---

<div class="flex flex-col items-center">
    <div class="lg:w-4/6 h-max relative flex flex-col items-center transition-all duration-300">
        <Navbar show={show} />
        <slot />
    </div>
</div>

<div class={show ? "bg-white h-screen w-full fixed left-0 right-0" : "hidden"}></div>
